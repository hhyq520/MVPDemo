package com.zhitengda.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.ztd.mvpstandardpro_as.bean.Siteinfo;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "SITEINFO".
*/
public class SiteinfoDao extends AbstractDao<Siteinfo, Long> {

    public static final String TABLENAME = "SITEINFO";

    /**
     * Properties of entity Siteinfo.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property _id = new Property(0, Long.class, "_id", true, "_id");
        public final static Property SiteCode = new Property(1, String.class, "siteCode", false, "SITE_CODE");
        public final static Property SiteName = new Property(2, String.class, "siteName", false, "SITE_NAME");
    }


    public SiteinfoDao(DaoConfig config) {
        super(config);
    }
    
    public SiteinfoDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"SITEINFO\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: _id
                "\"SITE_CODE\" TEXT," + // 1: siteCode
                "\"SITE_NAME\" TEXT);"); // 2: siteName
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"SITEINFO\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, Siteinfo entity) {
        stmt.clearBindings();
 
        Long _id = entity.get_id();
        if (_id != null) {
            stmt.bindLong(1, _id);
        }
 
        String siteCode = entity.getSiteCode();
        if (siteCode != null) {
            stmt.bindString(2, siteCode);
        }
 
        String siteName = entity.getSiteName();
        if (siteName != null) {
            stmt.bindString(3, siteName);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, Siteinfo entity) {
        stmt.clearBindings();
 
        Long _id = entity.get_id();
        if (_id != null) {
            stmt.bindLong(1, _id);
        }
 
        String siteCode = entity.getSiteCode();
        if (siteCode != null) {
            stmt.bindString(2, siteCode);
        }
 
        String siteName = entity.getSiteName();
        if (siteName != null) {
            stmt.bindString(3, siteName);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public Siteinfo readEntity(Cursor cursor, int offset) {
        Siteinfo entity = new Siteinfo( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // _id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // siteCode
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2) // siteName
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, Siteinfo entity, int offset) {
        entity.set_id(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setSiteCode(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setSiteName(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(Siteinfo entity, long rowId) {
        entity.set_id(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(Siteinfo entity) {
        if(entity != null) {
            return entity.get_id();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(Siteinfo entity) {
        return entity.get_id() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
